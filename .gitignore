# Godot
.godot/
*.translation

# .NET / C#
.mono/
[Bb]in/
[Oo]bj/
*.user
*.userprefs

# Raw asset downloads — large source material, never committed (see THIRD_PARTY.md)
assets/

# Runtime player saves — not part of the project.
# ANCHORED TO THE ROOT ON PURPOSE. Bare `saves/` matches at any depth, and Windows matches it
# case-insensitively - so it silently swallowed `content/Saves/`, the save system itself, and git
# never showed the files at all.
/saves/

# Internal design scratch kept out of the repo (spreadsheets, raw notes, name ideas)
_design_docs/

# OS
.DS_Store
Thumbs.db
